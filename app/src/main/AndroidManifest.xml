<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidtetris">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_androidtetris"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_androidtetris_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTetris">
        <activity
            android:name=".activity.main.TetrominoColorsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.tetris.TetrisActivity"
            android:exported="false" />
        <activity
            android:name=".activity.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>